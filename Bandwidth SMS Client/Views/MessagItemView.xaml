<UserControl x:Class="Bandwidth_SMS_Client.Views.MessagItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bandwidth_SMS_Client.Views"
             mc:Ignorable="d" >
    <Border Background="#FFDDE8DD" 
            Padding="10,10,10,10" 
            CornerRadius="5,5,5,5"
            Margin="2,2,2,2" Width="400">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock d:Text="From: +639213466856" >
                <Run Text="From: "/>
                <Run Text="{Binding MFrom}"/>
            </TextBlock>
            <TextBlock TextWrapping="Wrap" 
                       Text="{Binding Body}" 
                       d:Text="This is the body of the message" 
                       Grid.Row="1"/>
        </Grid>
    </Border>
</UserControl>
